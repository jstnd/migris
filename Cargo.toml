[workspace]
members = ["migris", "migris-cli", "migris-gpui"]
resolver = "3"

[workspace.dependencies]
migris = { path = "migris" }

# external dependencies
clap = { version = "4.5.54", features = ["derive"] }
serde = "1.0.228"
