[workspace]
members = ["migris", "migris-cli", "migris-gpui", "migris-ui"]
resolver = "3"

[workspace.dependencies]
migris = { path = "migris" }

# external dependencies
clap = { version = "4.5.54", features = ["derive"] }
